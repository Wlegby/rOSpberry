[package]
name = "rospberry"
version = "0.1.0"
edition = "2024"

[dependencies]
ttf-parser = { version = "0.25.1", default-features = false, features = ["no-std-float"]}

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
